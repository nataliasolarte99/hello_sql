
-- Gets all data from the "users" table
SELECT * FROM users;

-- Gets all names from the "users" table
SELECT name FROM users;

-- Gets all IDs and names from the "users" table
SELECT user_id, name FROM users;
